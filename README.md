CMake Protobuf Example
======================

Minimal example of using CMake to manage C++ project with protobufs.

Build
======================

    $ mkdir build
    $ cd build
    $ cmake ..
    $ make

Run
======================
    $ ./main
